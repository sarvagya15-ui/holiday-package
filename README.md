# holiday-package
it’s a stream file that contains a data mining workflow (a visual stream of nodes and connections). These .str files typically describe:  The data sources being used (like CSV files, databases, or datasets).  The operations and transformations applied to the data (Select, Type, Derive, Filter, etc.).  The modeling nodes.  The outputs 
